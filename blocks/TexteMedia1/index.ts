import View from './View'
import Edit from './Edit'
import { Availability, Block } from '../types'

const TexteMedia1: Block = {
    name: 'Texte + Media',
    availability: Availability.ALL,
    View,
    Edit,
}

export default TexteMedia1
